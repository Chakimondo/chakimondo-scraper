database:
  client: pg
  host: localhost
  user: kronodynamic
  password: krono
  database: kronodynamic
  port: 5432
crawler:
  root: https://pt.wikipedia.org
  robots: false
  sitemaps: false
  # Depth set to two only for debug (to permit links to finish and then close the crawler)
  depth: 1
  # depth: 9
dump:
  root: home # filesystem|home|relative
  path: .kronodynamic/database
